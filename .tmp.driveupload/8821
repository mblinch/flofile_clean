import 'package:flutter/material.dart';
import 'screens/caption_builder_screen.dart';
import 'widgets/api_test_widget.dart';
import 'widgets/game_test_widget.dart';
import 'widgets/calendar_widget.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Caption Writer',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: const Color(0xFF2E3A59),
          brightness: Brightness.light,
        ),
        scaffoldBackgroundColor: Colors.white,
        useMaterial3: true,
      ),
      home: const CalendarWidget(), // Temporarily show calendar
      // home: const GameTestWidget(), // Temporarily show game test
      // home: const CaptionBuilderScreen(),
      // home: const ApiTestWidget(), // Temporarily show API test
    );
  }
}
